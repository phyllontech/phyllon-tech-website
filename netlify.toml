[build]
  publish = "."
  command = "npm run build"

[functions]
  directory = "netlify/functions"

[dev]
  publish = "."
  port = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200