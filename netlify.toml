[build]
  publish = "."
  command = "npm run build"

[functions]
  directory = "netlify/functions"

[dev]
  command = "npm run dev"
  publish = "."
  port = 3000

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200